<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:icon="@drawable/ic_settings"
        android:title="@string/main_menu_options"
        app:showAsAction="always">

        <!-- Submenu -->
        <menu>
            <!-- see progress -->
            <item
                android:id="@+id/main_menu_progress"
                android:title="@string/main_menu_progress" />

            <!-- Sow initial slide check box -->
            <item
                android:id="@+id/show_animation_checkbox"
                android:checkable="true"
                android:title="@string/main_menu_slide" />

            <!-- Play sound effects -->
            <item
                android:id="@+id/play_sound_effects_checkbox"
                android:checkable="true"
                android:title="@string/main_menu_sound" />
        </menu>

    </item>
</menu>